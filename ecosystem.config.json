{
    "apps": [
    {
        "name": "market-dashboard",
        "script": "npm",
        "args": "run build && npx serve -s dist -l 80",
        "env": {
            "NODE_ENV": "production"
        }
    }
]
}
